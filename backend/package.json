{
  "name": "store-ratings-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "build": "echo 'Nothing to build for server'",
    "start": "node src/index.js",
    "prisma:dev": "npx prisma migrate dev --name init && npx prisma generate",
    "prisma:deploy": "npx prisma migrate deploy && npx prisma generate",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}
